A fork of  [golang-set](https://github.com/deckarep/golang-set)

## AND 

* a Scan(destinationSlice) method to populate data ta slice.
* a NewSetWithSlice(slice) method to init a Set using a typed slice like []int or []string
